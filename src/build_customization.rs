pub fn explain() {
    println!("Make sure to check Cargo.toml!");

    //Release profiles are predefined, customizable profiles
    //- have different configs that allow programmer more 
    //  control over how their code is compiled
    //All profiles are configured independently from each other

    //Cargo has two profiles: dev and release
    //Yk cargo build (--release)? That's a profile
    //Check toml 
}